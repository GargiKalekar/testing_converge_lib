import 'package:flutter/material.dart';

class TimingsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
   return Scaffold(body: Column(crossAxisAlignment: CrossAxisAlignment.center,children: [ElevatedButton(
      onPressed: (){}, child: Text("read example")),ElevatedButton(
      onPressed: (){}, child: Text("write example"))],),);
  }

}

